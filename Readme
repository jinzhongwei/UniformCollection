背景:
多个客户端消费消息队列的消息，会因为不同的ip的消息量不同而导致不同客户端负载不均衡，一种情况是做center的转发负载
另外一种解决方案就是动态的根据不通ip的消息量，来重组每个客户端的监听ip集合。
那么我们把问题抽象为一个如下的数学问题：
给出一个二维数组，每一维我们称之为集合，我们的目标是对二维数组进行重组，使得每个集合的总和尽可能接近，但是集合中的数量添加新的元素与减少元素都是有代价的，即为惩罚。
我们的目标是使得代价最少，我们用标准差作为二维元素的分布好坏评判标准。
接下来是一个实现的demo，改demo尽可能的搜索解空间，力求找到尽可能接近最优解的近似最优解。
